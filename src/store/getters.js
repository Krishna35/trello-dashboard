export default {
  lastListId(state) {
    return state.lastListId;
  },
  lastTaskId(state) {
    return state.lastTaskId;
  },
  lists(state) {
    return state.lists;
  },
  tasks(state) {
    return state.tasks;
  },
  overlay(state) {
    return state.overlay;
  },
  currentData(state) {
    return state.currentData;
  },
};
